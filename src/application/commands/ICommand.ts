/**
 * コマンドインターフェース
 * 
 * ユーザーの操作を表すコマンドの抽象化です。
 * すべてのコマンドはこのインターフェースを実装し、実行と取り消しが可能です。
 */
export interface ICommand {
  /**
   * コマンドを実行
   */
  execute(): void;

  /**
   * コマンドを取り消し
   */
  undo(): void;
}

